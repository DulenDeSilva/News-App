# News-App
This is a simple full-stack web application that allows users to search for the latest news articles. The project includes a Node.js backend API that fetches news data and a frontend interface for displaying the results.


🔧 Backend (Node.js + Express)

Provides an API endpoint to search for news.

Handles requests and interacts with external news sources.

Contains server.js, package.json, and necessary dependencies.

🌐 Frontend (HTML, CSS, JavaScript)

User-friendly interface to enter a search query.

Sends requests to the backend API.

Dynamically displays news results using JavaScript.

Styled with simple, clean CSS.

🎯 Features

Search news articles by keyword

Fetch articles through a Node.js API

Display results in a responsive UI

Frontend + backend structure for learning software architecture
